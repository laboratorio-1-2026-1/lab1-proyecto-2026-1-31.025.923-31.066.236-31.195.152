# lab1-proyecto-2026-1-31.025.923-31.066.236-31.195.152
SmartGym API

## Variables de entorno

- Copia el archivo de ejemplo y rellena tus valores locales (no subir credenciales):

	```bash
	cp .env.example .env
	```

- El proyecto ya carga variables con `dotenv` (comprueba `src/app.js`).
- No subas el archivo `.env` al repositorio. El `.gitignore` ya incluye `.env`.

- Variables usadas (ejemplo): `PORT`, `DB_HOST`, `DB_USER`, `DB_PASSWORD`, `DB_NAME`, `JWT_SECRET`.

Si necesitas instalar dependencias relacionadas a variables de entorno, ejecuta:

```bash
npm install
```
